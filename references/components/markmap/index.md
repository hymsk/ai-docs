# Markmap

Markmap 把 Markdown 标题和列表转换为可缩放的思维导图，适合知识树、模块分解、会议提纲和概念分类。

详细方法见 `hierarchy.md`。

## 适用边界

- 重点是父子层级，而不是节点之间任意关系。
- 层级最好控制在 3-5 层。
- 每个节点使用短语，不放长段落。
- 源码不能包含原始 HTML。

Markmap 在浏览器运行时渲染，并随页面主题和容器宽度重绘。构建后需浏览器检查。
